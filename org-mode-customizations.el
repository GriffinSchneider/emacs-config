(require 'init)

;; Align tags to column 90
(setq org-tags-column -90)

;; Hide leading *'s
(setq org-hide-leading-stars t)

;; Only use odd-numbered levels
(setq org-odd-levels-only t)


(provide 'org-mode-customizations)
